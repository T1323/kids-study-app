# LLM API 設定 (支援 OpenAI 相容介面，如 Google Gemini / DeepSeek 等)
# LLM_API_KEY= (不儲存 Key，由前端傳入)

# 若用其他服務，可改 baseURL，例如 DeepSeek: https://api.deepseek.com
LLM_BASE_URL=
# 模型名稱，例如 gpt-4o-mini、gpt-4o、deepseek-chat
LLM_MODEL=gpt-4o-mini

# 後端監聽 port
PORT=3000
